cmake_minimum_required(VERSION 2.8)
project( displayer.out )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( displayer.out displayer.cpp )
target_link_libraries( displayer.out ${OpenCV_LIBS} )
